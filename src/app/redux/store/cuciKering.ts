import { createSlice } from "@reduxjs/toolkit"


const initialState = {
    addPelanggan: []
}

export const CuciKeringSlice = createSlice({
    name: "cuciKering",
    initialState,
    reducers: {}
})